function add(x: number, y: number) {
  return x + y;
}

add(99, 88);

let stringArray: number[] = [1.2];
stringArray.push(5);

let myObject: {
  name: string;
} = {
  name: "Rupi",
};

let myFunction: Function = (a: number, b: number) => {
  return a + b;
};

myFunction(1);
